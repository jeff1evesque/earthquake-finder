/**
* target_earthquakes.js: contains global function(s) needed to determine an array
*                        of earthquakes within supplied restriction(s).
*/

/**
 * return_earthquakes: return an array of earthquakes within 'negdays', and 'radius'
 *                     of supplied origin.
 *
 * @origin_longitude, longitude value of the origin point.
 * @origin_latitude, latitude value of the origin point.
 * @radius, acceptable distance (mi) from the origin point (longitude, latitude).
 * @negDays, acceptable date range [-negDays, today] for return values.
 */
  var return_earthquakes = function(origin_longitude, origin_latitude, radius, negDays) {
  };

