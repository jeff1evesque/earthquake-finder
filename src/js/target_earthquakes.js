/**
* target_earthquakes.js: iterate over supplied geojson file, then return array
*                        of earthquakes within supplied restriction:
*
* @origin_longitude, longitude value of the origin point.
* @origin_latitude, latitude value of the origin point.
* @radius, acceptable distance (mi) from the origin point (longitude, latitude).
* @negDays, acceptable date range [-negDays, today] for return values.
*/

/**
 * target_asset: converts degrees to radians.
 */
  var target_asset = function(origin_longitude, origin_latitude, radius, timeframe) {
  };

